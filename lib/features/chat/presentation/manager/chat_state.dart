part of 'chat_bloc.dart';

@immutable
class ChatState {
  // final PageState<List<ChatItemViewModel>> chatState;

  const ChatState(
      // {

      // this.chatState = const PageState.init(),

      // }
      );

  ChatState copyWith(
      // {
      // PageState<List<CartItemViewModel>>? chatState,

      // }
      ) {
    return const ChatState(
        // chatState: chatState ?? this.chatState,

        );
  }
}
